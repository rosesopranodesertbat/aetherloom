{
  "name": "aetherloom",
  "version": "1.0.0",
  "description": "A flying-carpet wizard duel. WebGPU renderer, Rust simulation compiled to WebAssembly.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "test": "npm run build && npm run test:run",
    "test:run": "npm run test:models && npm run test:contracts && node test/balance-sim-test.mjs && node test/headless-render-test.mjs",
    "test:models": "node test/model-preview-test.mjs",
    "test:contracts": "node test/combat-render-contract-test.mjs"
  }
}
